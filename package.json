{
  "name": "income",
  "private": true,
  "version": "0.0.0",
  "description": "Income calculate",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^0.9.0"
  },
  "scripts": {
    "prestart": "npm install",
    "start": "http-server -a 192.168.0.9 -p 8000 -c-1"
  }
}
